[package]
name = "threading-bench"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
tokio-stream = "0.1"
criterion = "0.5"

[[bench]]
name = "throughput"
harness = false
